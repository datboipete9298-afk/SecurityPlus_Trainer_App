# Copy to .env for local AI server (never commit .env)
OPENAI_API_KEY=your_api_key_here
# Optional: model id (default gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
# Optional: Express AI server port (default 8787)
# AI_SERVER_PORT=8787
