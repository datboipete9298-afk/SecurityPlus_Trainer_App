# PDF files

**You do not need to put files here for production use.**

Add your licensed PDFs through the in-app **PDF setup** page (`/pdf-setup`). They are stored in **IndexedDB** in your browser — not on the server.

This folder is reserved for optional local/dev static hosting only. **Do not commit `.pdf` files** to the repository.
